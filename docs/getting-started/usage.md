# Usage

- Configure `relicbound-paper/src/main/resources/config.yml` as needed.
- Start your Paper server and verify the plugin loads.
- Use in-game commands and follow the in-game help for archetype selection.
