dependencies {
    implementation project(':relicbound-core')
    def paperApiVersion = project.hasProperty('paperApiVersion') ? project.property('paperApiVersion') : '26.1.2.build.64-stable'
    compileOnly "io.papermc.paper:paper-api:${paperApiVersion}"
}
