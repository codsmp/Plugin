dependencies {
    implementation project(':relicbound-core')
    def paperApiVersion = project.hasProperty('paperApiVersion') ? project.property('paperApiVersion') : '26.1.2-R0.1-SNAPSHOT'
    compileOnly "io.papermc.paper:paper-api:${paperApiVersion}"
}
